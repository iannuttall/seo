interface:
  display_name: "Query cluster"
  short_description: "Cluster retained GSC queries by token overlap"
  default_prompt: "Use $query-cluster to run the query-cluster report and explain its evidence and limits."
