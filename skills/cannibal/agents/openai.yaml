interface:
  display_name: "Cannibal exposure"
  short_description: "Find queries exposed across multiple URLs"
  default_prompt: "Use $cannibal to run the cannibal report and explain its evidence and limits."
