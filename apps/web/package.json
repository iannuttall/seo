{
  "name": "@seo/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm build:discovery && pnpm build:sitemap && astro build",
    "build:discovery": "node scripts/build-agent-skills.mjs",
    "build:sitemap": "node scripts/build-sitemap.mjs",
    "clean": "rm -rf .astro dist .wrangler public/.well-known/agent-skills",
    "deploy": "pnpm build && wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "dev": "astro dev",
    "predev": "pnpm build:discovery",
    "lint": "astro check",
    "preview": "astro preview",
    "test": "node --test scripts/site.test.mjs",
    "typecheck": "astro check"
  },
  "dependencies": {
    "astro": "^7.0.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.9.3",
    "wrangler": "^4.110.0"
  }
}
